{
  "name": "padev-phpreload-docker",
  "version": "1.0.0",
  "description": "<!-- command inspect config image -->\r docker-compose --env-file .env.dev config",
  "main": "index.js",
  "scripts": {
    "padev": "node ./fsreload.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.0"
  }
}
