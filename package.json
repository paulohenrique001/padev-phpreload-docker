{
  "name": "livereload-docker",
  "version": "1.0.0",
  "description": "<!-- command inspect config image -->\r docker-compose --env-file .env.dev config",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.0"
  }
}
